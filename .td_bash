#!/bin/bash

target_dir=$(cat .dir_cache | fzf)
if [ -n "$target_dir" ]; then
  cd "$target_dir" || exit
  # Use parameter expansion to get the basename safely
  session_name=$(basename "$PWD")
  tmux new -A -s "$session_name"
else
  echo "No directory selected."
fi

